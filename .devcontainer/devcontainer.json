{
  "name": "AdjointNonlinearRayTracing",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "runArgs": [
    "--gpus=all",
    "--shm-size=1g"
  ],
  "remoteUser": "vscode",
  "overrideCommand": false,
  "init": true,
  "containerEnv": {
    "CUDACXX": "/usr/local/cuda/bin/nvcc"
  },
  "postCreateCommand": "git config --global --add safe.directory '*' && git submodule sync --recursive && git submodule update --init --recursive --jobs 8 && mkdir -p build && cd build && cmake .. -G Ninja -DCMAKE_BUILD_TYPE=Release -DENOKI_CUDA=1 -DENOKI_AUTODIFF=1 -DENOKI_PYTHON=1 -DCMAKE_CUDA_COMPILER=$CUDACXX -DPYTHON_EXECUTABLE:FILEPATH=$(which python) && cmake --build . -j $(nproc) && cd .. && echo 'source $(pwd)/setpath.sh' >> ~/.bashrc",
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-vscode.cmake-tools"
      ]
    }
  }
}
